# Claude IDE
.claude/

# Cursor IDE
.cursor/
.cursor-index/
.index/
.specstory
.cursorindexingignore
CLAUDE.md

# Go
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/
node_modules/

# Go workspace file
go.work

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Build output
bin/
dist/
build/
backend/sdk/ts/dist/
backend/renderops-api
backend/app
guest-agent/guest-agent
guest-agent/guest-agent-*
guest-agent/guest-agent-new

# IDE - VS Code
.vscode/
*.code-workspace

# IDE - GoLand
.idea/

# IDE - Vim
*.swp
*.swo
*~

# OS - macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# OS - Linux
*~

# OS - Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Project specific
# Logs
*.log
logs/

# Environment variables
.env
.env.local
.env.*.local
vmrenderopsnp01_key.pem

# Local/private planning and integration config
aws_cost_planning.csv
testing/.env

# Docker
docker-compose.override.yml

# Temporary files
tmp/
temp/
*.tmp

# Firecracker artifacts
*.ext4
assets/initramfs.cpio.gz
vmlinux
firecracker-socket
# Locally-seeded VM assets + the pushed bundle (see `make seed-assets` / `make bundle`)
assets/firecracker
assets/manifest.sha256
renderops-assets.tar.gz
# Firecracker binary archives
firecracker-*.tgz
*.tgz
# Firecracker release directories
release-*/
# Local test configuration
test_config.json

# Sensitive test scripts (may contain API keys or credentials)
test_supabase.sh
test_auth.sh
test_session.sh
test_sdk.py
test_api.sh
test_node.sh
guest-agent/test_node_bridge.sh

# Test coverage
coverage.txt
coverage.html

# Local development
local/
dev/
dev.sh

# Architecture notes (auto-updated, not for review)
notes.md
.notes/
PRODUCTIONIZATION_CHECKLIST.local.md
findings_global.md

# secret/backup files — never commit
.env.bak*
*.bak
*.pem
renderops-orchestration-machine.pem
control-vpn.txt

# Confidential business docs (AWS credits requirement/storage sheets) — never commit
*.xlsx
*.xls
Requirement Gathering_sheet_.xlsx
Storage and Backup Questions.xlsx

# compiled binaries (built per-deploy, never committed)
backend/renderops-control-plane
backend/template-builder
backend/worker
